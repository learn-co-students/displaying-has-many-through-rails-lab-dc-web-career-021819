<h1>Doctor Show Page</h1>
<!--  displays doctors name, department, and appointments with
 each appointments date, time, and patient
 name(linking to the patients show page)-->
<h2>Name: <%= @doctor.name %></h2>
<h2>Department: <%= @doctor.department %></h2>
<br>
<h3>Appointments: </h3>
<ul>
  <% @doctor.appointments.each do | appointment | %>
    <li><%= appointment.appointment_datetime.strftime("%B %d, %Y at %R") %>,
    <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>
