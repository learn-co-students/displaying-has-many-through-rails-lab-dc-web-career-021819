<h1><%= @patient.name %>'s Page</h1>
  <ul>
    <% @patient.appointments.each do |a| %>
      <li><%= (a.appointment_datetime).strftime('%B %d, %Y') %> at <%= (a.appointment_datetime).strftime('%H:%M') %> with <%= link_to a.doctor.name, doctor_path(a.doctor) %></li>

    <% end %>
  </ul>
